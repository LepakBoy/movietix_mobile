import React, {useState} from 'react';
import s from './style';

import {
  ScrollView,
  View,
  Text,
  TouchableOpacity,
  Image,
  TextInput,
  Button,
} from 'react-native';

function OrderHistory() {
  return (
    <ScrollView>
      <Text>asdas</Text>
    </ScrollView>
  );
}

export default OrderHistory;
