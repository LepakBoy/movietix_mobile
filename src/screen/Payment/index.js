import React, {useState} from 'react';
import s from './style';

import {
  ScrollView,
  View,
  Text,
  TouchableOpacity,
  Image,
  TextInput,
  Button,
} from 'react-native';

function Payment() {
  return (
    <ScrollView>
      <View>
        <Text>payment</Text>
      </View>
    </ScrollView>
  );
}
export default Payment;
